export enum EmailErrors {
    noEmailProvided = 'No email was provided',
    badEmailFormat = 'Bad email format provided',
    didntSent = 'Something went wrong while sending emails'
}

export enum RateErrors {
    rateFetchFail = 'We were unable to fetch the rate'
}
